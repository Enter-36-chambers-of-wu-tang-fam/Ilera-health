import React, { Component } from 'react';

export default class Notes extends Component {

    constructor (props){
      super(props)
    }

    render() {
      return (
          <div>
          Hello from Notes!
          </div>
      );
    }
}